Multiple select dropdown components

You can run this project as below to see how the component behave
1. npm install
2. npm run start // it will start a server

Then you will be redirected to a demo html file where the component is being used.